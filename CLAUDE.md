# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## MALACA ERP build tracker (LOCAL-ONLY — no deploy until told)

Scope source of truth: `MALACA_GAP_ANALYSIS.md` (+ client questions in `CLIENT_QUESTIONS.md`). Client-facing ZH copies: `MALACA_功能进度与待办_客户版.md`, `客户确认问题清单.md`. **Keep this tracker current** — tick items as they land.

**Mode: DEPLOYED.** Prod (`www.erp-melaka.com`, site `frontend`) tracks OSI `main` — at `df1ae92` (2026-07-21, Shopee-mirror stages wave; build+migrate run — v22 ship-date backfill 0 rows expected/v23 completed 3715 + cancelled 1621; scheduler 2522526 / worker 2522655). `ship_date_local` accrues as active orders resync `pickup_done_time` — Shipping tab's ship-date Today view fills over the coming days; historical shipped orders stay dateless. FX seeded on prod: Currency Exchange `VND→CNY 0.000258`, `PHP→CNY 0.1098` (backdated 2026-01-01) via `devtools/fx_seed.py`. **`OSI Settings.enable_erpnext_sync` = 0 on prod** (turned off client-side after the July error flood) — ERP docs won't book until re-enabled; FX is in place for when it is. Future work: build + verify on the WSL dev bench first; push/deploy only when the user says so. (Translations `merge` still runs at every commit per the rule below.) Post-deploy pending: assign new OSI roles + per-store User Permissions to real prod portal users.

### Done (shipped in OSI)
Shopee order/product sync · SKU label overlay · ERPNext SO/DN/SI creation · return reversal (credit note + return DN) · multi-currency (per-currency receivables, fail-loud FX) · HTTP retry/429/token-refresh · ZH i18n pipeline · ERP resync (single batch job) · allow-negative-stock.

### Wave 1 — low risk (building now)
- [x] **Manual review gate + 刷单 flag** (config-toggled). `OSI Settings.require_order_review` (default OFF). `Online Sales Order` fields: `review_status` (pending/approved/rejected/on_hold), `reviewed_by/at`, `is_special_order`, `special_order_reason`; `erp_sync_status` += `awaiting_review`. Gate in `order_service._upsert` holds un-approved/special orders out of ERP doc creation. API `api/review.py` (approve/reject/flag/summary). UI: list bulk actions + form buttons (`_list.js` / `.js`). Patch `v13` (existing orders → approved, never retro-block). **Verified on mock:** 90 pending held, `held_with_sales_order=0`, approve flips pending→approved. zh queued in `zh.pending.csv`.
- [x] **发货 / 退货 summary reports** — Script Reports `OSI Shipping Summary` + `OSI Return Summary` under `report/`. Filters: from/to date, period (Day/Month/Year), store, platform. Group by period × store × currency; cols Period/Store/Region/Warehouse/Currency/Orders(Returns)/Units/Value; total row; CSV/Excel export built-in. **Verified on mock:** shipping 4 rows multi-currency (MYR/PHP/VND), returns 1 row. zh queued.
- [x] **Multi-locale translation scaffold** — `scripts/osi-translations.py` now multi-locale (`merge` folds every `<lang>.pending.csv` → `<lang>.csv`; `scaffold` creates missing locale CSVs; `check` per-locale). Shipped empties `tl/th/vi/ms/hi/ja/es` alongside `zh`. Verified: non-zh fold works (th test). **Wave 1 complete.**

### Wave 2 — medium (money / external API)
- [x] **Escrow fee ingest** — `api/shopee/payment.py` `get_escrow_detail` (real v2) + `api/services/escrow_service.py` (maps `order_income` → Online Sales Order fields: escrow_amount/buyer_total/commission/service/transaction/fees_total/escrow_synced_at/escrow_payload_json) + `api/escrow.py` (single + batch enqueue + summary) + form "Sync Fees" button. Mock route `mockshopee/routers/payment.py`. **Verified on mock:** 75 synced, gross−fees==escrow exact. SHOPEE_API_NOTES Payment section added.
- [x] **Daily P&L report** — Script Report `OSI Profit and Loss`: period Day/Month/Year × store × currency; cols Gross Sales / Platform Fees (escrow) / Net Payout / COGS / Profit / Margin %; COGS = sum(qty × Item.valuation_rate) → 0 until real per-SKU costs loaded (client Q15); ad-spend/opex out of scope. **Verified on mock:** gross−fees==net, 91% margin (pre-COGS).
- [x] **Ship-confirm reconciliation worklist** — Script Report `OSI Ship Confirm Reconciliation` (one row per order, links to OSO/SO/DN/credit note). Buckets: paid-not-shipped (paid, no DN, no shipped_at, not dead) + shipped-then-cancelled (cancelled/refunded with DN or shipped_at; hides already-reversed unless `include_reversed`). Filters: bucket/store/platform/from-to/min_age_days. Summary pills + bucket chart; days>7 highlighted red. Dev check `devtools/recon_check.py`. **Verified on live site data:** 226 orders → 2 paid-not-shipped + 1 shipped-then-cancelled (DN, no CN) correctly flagged. zh queued.
- [x] **Return disposition** (restock / damaged / scrap). OSO fields `return_disposition` + `disposition_stock_entry` (read-only, one-shot — change after stock moved throws); `OSI Settings.damaged_warehouse`. restock = no stock doc (return DN already restocked); damaged = Material Transfer → damaged warehouse; scrap = Material Issue. API `api/disposition.py` (set_disposition/summary) + form Disposition buttons (show when return DN set & undecided). Dev check `devtools/disposition_check.py` (+ `make_returns`/`repair_crosslinked`). **Verified on mock: all 5 checks PASS.** zh queued+merged. **Bonus fix (15dc19a):** existing-doc lookups in `erpnext_sales_service` matched credit notes / return DNs / cancelled docs (return docs copy `sales_order` refs) → crosslinked 9 dev orders, broke reversal with "quantity must be negative number"; now filtered `docstatus < 2 AND is_return = 0`. full_e2e 6/6 PASS post-fix.
- [x] **COGS manual entry per SKU** (5b7c123) — `Online Product.unit_cost` (store currency, in list view, resync-safe); P&L COGS = qty × unit_cost, fallback Item.valuation_rate. **Verified: cogs == cost × units exact.**
- [x] **`OSI Cost Entry` doctype** (5b7c123) — posting_date / optional store / cost_type (ads, domestic+intl logistics, opex, FX, other) / currency (autofill from store revenue_currency) / amount. P&L "Manual Costs" col merged period×store×currency; storeless costs = own rows; **Profit = Net − COGS − Manual Costs**. Dev check `devtools/pnl_check.py` 7/7 PASS. zh merged. **Wave 2 complete.**
- [x] **Platform + SKU dimensions** on shipping/return summaries — Platform column always-on; `Detail by SKU` check filter (child-item join, value from `line_total`); default mode output unchanged. `devtools/sku_col_check.py` 12/12 PASS. **Wave 2 fully complete.**

### Wave 2.5 — new client requirements (spec 1.0.1 + answer sheet, see MALACA_GAP_ANALYSIS.md v2)
- [x] **Audit-trail hardening** (da4efea) — track_changes on OSO/Store/Product/Settings (Versions on save); `api/utils/audit.py` timeline comments for set_value transitions (approve/reject/flag/disposition). `devtools/audit_check.py` 2/2 PASS.
- [x] **1000/day certification** (b689ffe) — `devtools/cert.py` wave-enqueue runner (queue cap 550). **PASS: 1068 orders full pipeline in 491s = 2.17/s ≈ 188k/day projected, 0 deadlocks/lock-waits.** Report: `PERF_CERT_1000.md`.
- [x] **tl/th/vi/ms translations populated** (b689ffe) — full 548-string catalogs each (AI-drafted, need native spot-check). Launch set zh/en/tl/th/vi/ms complete. **Wave 2.5 complete.**
- [x] **Packaging audit** — `devtools/packaging_audit.py`; all reports/notifications/print-format/dashboard-charts/workspaces = standard module docs, roles/sidebars/HTML blocks = fixtures. Fixed: `OSI Connection Settings Workspace` sidebar added to fixtures. **PACKAGING OK** — fresh install reproduces full app.

### Wave 3 — large (client-reprioritized: Lazada/TikTok DEFERRED, Shopee-only v1)
- [x] **Roles / permissions / data isolation** — 4 roles (OSI Admin / OSI Ops Manager / OSI Ops Specialist / OSI Finance; legacy OSI User kept) + store-grain isolation via User Permission on Online Store / Department (incl. descendants) / Company / Warehouse (union semantics; System Manager/Administrator/no-UP users unrestricted → workers + e2e unaffected). `api/utils/isolation.py`; `permission_query_conditions` + `has_permission` hooks on 5 doctypes; `Online Store` += `company`/`department`; all 6 reports row-filtered + role-gated (P&L/gross-sales = Finance+Admin); all 49 whitelisted endpoints role-guarded + store-scoped (settings/auth/token export = admin-only); patch `v14` company backfill. `devtools/perm_check.py` **8/8 PASS**. Client doc `PERMISSIONS_DESIGN.md`.
- [x] **Implementation-status showcase** — single registry `api/feature_registry.py` (14 shipped / 5 planned, bilingual) renders as "Status 实施进度" section at top of `/osi-docs` (+ endpoint `api/status.get_implementation_status`). **Home auto-popup removed by client request** (`ecommerce_workspace_beta.js` now a stub) — status viewed in the user guide only. Docs UI-accuracy audit done: 9 misalignments fixed (real button labels: Link/Reconnect Store, Approve/Reject (Review), Flag/Unflag Special, Resync ERP (parked), Batch Link Items, Print AWB Labels; no Sync-Stores-from-Partner button — API-only; real order statuses; real Settings section names). `docs_check` 8/8 PASS.
- [x] **In-app user docs + API reference** — login-required www pages `/osi-docs` (12 bilingual EN/中文 feature guides with desk-mock walkthrough panels, client-side language toggle) + `/osi-docs/api` (rendered from `api_manifest.json`; params/roles/curl/frappe.call examples per endpoint). Generator `scripts/osi-apidocs.py` (AST scan, zh overlay `scripts/api_zh.json`, `--check` staleness mode). eCommerce workspace shortcut "User Guide 用户手册". `devtools/docs_check.py` **7/7 PASS**.
- [ ] **AI customer service module** (Duoke-AI-style multi-store chat, spec 1.0.1) — XL, phase to be confirmed with client
- [~] **Vendor-neutral refactor** (281e3cd): `api/adapters/` — `PlatformAdapter` ABC + `get_adapter(platform)` registry + `ShopeeAdapter` (thin delegation; Shopee payloads = canonical shape, other adapters translate into it). All neutral code (services/tasks/sync/webhook/auth/pdf-utils) now vendor-import-free; `_sync_stores_job` store query no longer hardcodes shopee. full_e2e 6/6 PASS through seam. **Remaining: Lazada adapter → TikTok adapter** (need client API creds + mock routes).
- [ ] Procurement tracking + replenishment
- [ ] AI self-evolving translation pipeline
- [ ] Automated 刷单 detector

### Wave 4 — seller portal + live ops (SHIPPED + DEPLOYED, `ad62f11`…`ea05d71`)
Client portal feedback source: `OSI edits.docx` (2026-07-16) — supersedes strict approve/reject wording; gap analysis updated in root working tree (uncommitted).
- [x] **Seller portal `/osi-portal`** (rebranded from `/malaca`; `website_redirects` map `/malaca` + `/melaca` → `/osi-portal`). Login-gated www shell (`www/osi-portal/`), vanilla-JS SPA (`public/osi-portal/app.js|css` — needs `bench build` after edits). 12 pages: Dashboard / Orders / Products / Inventory / Shipments / Customer Service (preview stub) / Finance & P&L / Reports / Stores / Settings / Access / Roadmap. `api/portal.py` = 19 whitelisted read/aggregate endpoints gated by `_require_portal()` (PORTAL_ROLES) + finer FINANCE_ROLES (P&L/costs/unit-cost) and MANAGER_ROLES (SKU / Item link / batch link); rows auto-filtered by `permission_query_conditions`, writes via `assert_store_access` + existing OPS endpoints (review/disposition/sync labels). Live-feed poll `api/portal.poll` 8–12s. Portal i18n: market langs zh/ms/th/vi/fil/hi/ja/es (patch `v17` enables Language records; **`tl.csv` renamed → `fil.csv`**); `devtools/portal_translation_check.py` fails on missing zh. Checks: `devtools/portal_check.py`.
- [x] **Order-check model change** (client: lightweight worklist, not strict approve/reject). `api/services/order_state_service.py` predicates (`needs_check`/`is_review_exempt`/`review_is_satisfied`/`can_request_label`); `review_status` += `not_required` (auto for fulfilled/closed orders); portal shows Needs Check + bulk **Mark Checked** (`api/review.mark_orders_checked`); reject kept out of portal; label readiness independent of check. OSO field `platform_order_status` (exact marketplace status, patch `v20` backfill).
- [x] **Shopee live push** — `api/webhook.shopee_push_webhook` (guest POST): acks URL-verification handshake pre-signature, multi-scheme HMAC-SHA256 verify (`api/shopee/push.py`, body-only or url|body × candidate URLs, shopee_push_key or partner key), order-status pushes (code 3) enqueue deduped RQ ingest. Check: `devtools/push_check.py`.
- [x] **Shipping-label queue + dispatch register** — OSO `shipping_label_*` fields (status: not_requested/queued/checking/waiting_allocation/generating/ready/failed + file/error/timestamps, `package_number` split from tracking, patch `v19`); `api/sync.queue_shipping_labels` + status polling; cron `retry_waiting_shipping_labels` (every min, 30-min window: expire stale, requeue). `api/dispatch_register.py` shipping manifest (shows store display name).
- [x] **Tracking backfill** — `api/services/logistics_service.py` (refresh on status change + hourly bounded `refresh_missing_tracking_numbers` cron).
- [x] **Desk new-order alerts** — `public/js/osi_new_order_alert.js` (batched toast per store + ding), cron `notify_new_orders` every min (Notification Log + realtime, checkpoint dedup); `OSI Settings.enable_new_order_alerts` (patches `v15`/`v16` default-on; `v16` fixes unset-Check-field detection via tabSingles row absence). Patch `v18` defaults `auto_create_items` on.
- [x] **Escrow auto-refresh** — cron `refresh_missing_escrow` */15min bounded batch; ingest stays queue-safe (no per-order jobs). Check: `devtools/escrow_auto_check.py` (9-case truth table).
- [x] **Admin purge tooling** — `api/maintenance.py` `purge_order_data` (role-gated + confirmation; deletes OSO + ERP chain + cost entries, keeps stores/products); `devtools/purge_orders.py` / `purge_check.py`. Store reconnect reuses existing store (`ea05d71`).
- [x] **Label print-window fix + ingest hardening** (`b5c3e37`, DEPLOYED 2026-07-17) — Shopee PROCESSED/RETRY_SHIP now inside the print window (was treated post-ship → nothing was ever printable; `LABEL_CLOSED_STATUSES` split from `REVIEW_EXEMPT_STATUSES`); single canonical `PLATFORM_STATUS_MAP` + `normalize_order_status()` (raw fallthrough used to write `to_return` into the Select and crash every push save; `to_return` now a real option → returns stage); portal `_stage` routes processed → toprint; `auth._apply_shop_info` skips colliding `integration_key` (IntegrityError was rolling back region → stores unsyncable); webhook falls back to long queue on default-queue over-capacity; portal UX batch (label reason captions in tables, ERP drawer card wired incl. retry, keyboard a11y, th contrast, poll-exhaustion notice, i18n leaks, stage/no-stores empty states). New `devtools/label_check.py` **13/13 PASS** (mock green path: queue→tracking→create→READY→PDF); `devtools/fx_seed.py` (explicit-rate FX seeding); full_e2e rewritten for Mark-Checked model (**6/6 PASS**), portal_check aligned; mock gained `logistics.py` router + boot-time SN counter (no cross-restart collisions). Prod verified: all 41 PROCESSED orders have real tracking → printable.

- [x] **Selectable label format (thermal 4×6 / A4)** (`f2bafc5`, DEPLOYED 2026-07-17) — `OSI Settings.default_label_format` (suggest/thermal/normal, unset⇒suggest) + `Online Store.label_document_type` per-store override; `queue_shipping_labels(document_type=…)` → job picks explicit > store > settings > Shopee suggestion, always intersected with the order's `selectable_shipping_document_type` (unavailable preference falls back, never fails). Portal: persistent compact picker (Auto/Thermal/A4, localStorage) in batch bar + drawer + Shipments header. SKU overlay/crop already format-aware (rules keyed region × document_type; bbox crop). `label_check` 14/14 incl. explicit-A4 pass (type persisted, stamped PDF). Prod: workers restarted on new job signature. Also fixed same-day (`801f227`): portal stage counts uncapped (was frozen at 2000) + live-poll no longer clobbers filter inputs/date panel (isEditing render guard).

- [x] **Region-local report dates + Dispatch Register rename** (`158d478`+`928e440`, DEPLOYED 2026-07-17) — dashboard Today tile now `order_date_local` per store (prod verified: dashboard 76 == orders 76, was 70 vs 74); `helpers.region_shifted_sql`+`REGION_UTC_OFFSET_HOURS` shift ship-date attribution in dispatch/shipping/returns/P&L to store clocks (recon uses `order_date_local`); portal card renamed "Dispatch Register — one row per shipped SKU, by ship date"; pnl_check made self-cleaning (marker-entry pollution false-failed reruns); dev mock result route now REQUIRES per-item shipping_document_type (root repo, uncommitted). pnl 6/6, sku_col 12/12, label, portal checks PASS.
- [x] **One date model for reports** (`c47826e`, DEPLOYED 2026-07-17) — Day/Month/Year seg removed (clashed with topbar date filter; Today never reached reports). Global filter drives reports+finance via `preset`; `_resolve_report_window` derives grouping: ≤31d→Day, ≤~12mo→Month, longer→Year, Any→unbounded Month, default→today/Day. `period` param kept ignored (compat). Verified: window table, shipping today rows =today Day-grouped, portal_check PASS.
- [x] **Store-local dates + Today default** (`063b7a2`, DEPLOYED 2026-07-17: pull+build+**migrate** run, worker restarted pid 2200462. Prod verified: backfill 5394/5394, **597 orders (11%) were on the wrong UTC day** now correct, Today=70 vs Any=5394, default==today; HTTP marshalling of `preset` verified on dev via authenticated curl; prod serves v-9 assets w/ datePreset wiring) — `created_at` is naive UTC; Today filter cut VN pre-07:00 orders + counts ignored dates (5k+ shown). New `OSO.order_date_local` (Date, indexed; ingest from create_time epoch × region tz via `helpers.REGION_TIMEZONES`/`epoch_to_region_date`/`region_today`; patch v21 backfill). Portal filters on local dates; `preset=today` resolved server-side PER STORE (mixed regions exact at midnight); orders page defaults Today (server default too); stage counts + live poll scoped to active date filter. Reports still group by site-day — future pass if wanted. Verified: backfill 328/328, epoch math, counts today=90 vs any=331, portal_check+label_check PASS.
- [x] **Invisible-stamp render fix** (`efe6f44`, DEPLOYED 2026-07-17, worker 2198186) — SKU stamp was in the PDF text layer but didn't RENDER: Shopee's SPX label content stream ends inside a translated/clipped graphics state; appended overlay inherited it → drew off-canvas. `_append_text_overlay` now sandwiches original content in `q`/`Q` and draws in default page space. Verified by rasterising the real prod label (poppler now installed in WSL; `Read` on PNG for visual checks — text-layer asserts alone are NOT enough for stamping).
- [x] **Typed result-poll fix — labels CONFIRMED WORKING on live Shopee** (`4bdb3ce`+`6da81e4`, DEPLOYED 2026-07-17) — live-probe on stuck prod order exposed the true blocker: `get_shipping_document_result` calls lacked `shipping_document_type` per item → Shopee rejected EVERY poll (`shipping_document_should_print_first: Detail: shipping_document_type`) → create succeeded but READY never seen → universal timeout. Mock accepted untyped polls (dev stayed green — mock now needs tightening). Fixes: typed order_list in job poll + continuation probe + desk results endpoint; probe-before-create (retries no longer restart Shopee generation); per-row fails park via classifier; restored READY branch eaten by an edit slip. **Prod verified: ORD-07030/31/33/34 all fetched→ready through the full pipeline.** 8 old orders correctly terminal ("parcel has been shipped" — labelled via Seller Centre while printing was broken). TODO: make mock require shipping_document_type on result route so this class can't regress silently.
- [x] **Label resilience round** (`07e37d3`, DEPLOYED 2026-07-17, prod worker/scheduler restarted pids 2181682/2181537) — prod findings: real Shopee doc-gen takes minutes (sync 12s poll → "taking longer than expected" failures) + overlapping fetch clicks raced → `shipping_label_file` pointed at deleted File → print "could not be read" (looked like stamp-not-applied). Fixes: timeout + "still processing/download later" → `waiting_allocation` (cron auto-retries 30min, portal flips to Print automatically); cron requeues preserve requested format; per-order `filelock` in `download_shipping_label_now` kills the race; print + stamp-editor sample picker self-heal stale pointers (fall back to newest attached `shopee-awb-*` File + repoint); editor preview now crops like print (Verify == Print). 13 stranded failed prod orders kicked to waiting_allocation. **Ops rule: prod `logs/worker.log` needs `chown frappe` before worker relaunch (root-touched).**
- [x] **Raw label storage + print-time stamping + refetch** (`5d443ce`, DEPLOYED 2026-07-17, prod workers+scheduler restarted pids 2179636/2179532) — label pipeline stores **RAW** Shopee PDF (`_download_single_awb(stamp=False)`; fixed param-shadowing: local filename-timestamp `stamp` var overwrote the flag → always stamped); new `api/sync.print_shipping_labels` stamps stored raws with CURRENT rules + crop at print time, merges batch → one PDF (stamp fixes apply retroactively; desk batch download flow unchanged). Portal: Fetch Label → Print two-step; batch/Shipments = "Fetch Labels" + "Print (N)"; **Refetch** (`download_shipping_label_now force=1`, replaces raw + culls old File) in drawer + table icon — fixes wrong-format dead end. Day/Month/Year seg moved to topbar (Reports+Finance, single state). label_check PASS (+raw filename & print steps), portal_check PASS. Note: dev bench + prod workers must be restarted on label-code changes (stale-worker race found here).
- [x] **Visual SKU stamp editor** (`c4724ae`, DEPLOYED 2026-07-17) — Shipments "Stamp layout" button (admin-only) → modal editor for `OSI Settings.sku_overlay_rules`: region select × Thermal/A4 seg, latest saved label PDF rendered via **vendored pdf.js** (`public/osi-portal/vendor/`, lazy-loaded; blank true-size page fallback), drag SKU box (touch + arrow-key nudge 0.5/2mm, live mm readout), font/max-lines steppers, "Verify stamp" = real engine pass on sample → stamped PDF tab, save upserts (region,type) rule; chips show configured pairs. Backend `api/portal.py`: `get_sku_stamp_config`/`save_sku_stamp_rule`/`preview_sku_stamp` (ADMIN_ROLES); `overlay_sku_qty_on_pdf` gained explicit-`rule` override param. `devtools/stamp_editor_check.py` **8/8 PASS**; label_check+portal_check regression PASS. Prod store_name fields verified already real (docname leak was UI-only).
- [x] **Portal global filters + multi-store + real store names** (`f465f46`, DEPLOYED 2026-07-17) — (1) docname leaks fixed: dashboard top-stores / products / inventory / returns now map through `storeName()` (docname frozen by autoname `format:{store_name}-{#####}`; **post-deploy: refresh `fetch_shopee_store_info` per prod store** to replace placeholder store_name values). (2) Topbar global date-range (+ Last 90 days) & sort (newest/oldest/store/order) applied to Orders/Shipments/Reports/Finance/Returns; per-page date panel + reports sort select removed; dashboard stays live-unfiltered; `get_orders` += sort_by, `get_returns` += from/to, finance wired to range. (3) Store switcher multi-select → CSV param; `_store_filter` expands to IN; all 5 reports accept docname/CSV/list via `isolation.as_store_tuple` (comma-safe); totals exact (SQL-level). Verified: 2-store scoping exact across orders/reports/pnl/dispatch/finance, portal_check PASS, perm_check 8/8 PASS.
- [x] **Portal label UI stripped to derived Download button** (`d508a78`, DEPLOYED 2026-07-17) — client asked to drop the front-facing status machine and "dial in stability". One rule: label saved OR fetchable now (tracking + print window) → Download Label button; else nothing, no error copy. New sync endpoint `api/sync.download_shipping_label_now` (click-time create→poll(6×2s)→download inline; cached file served instantly; `generate_shipping_label_job` gained `max_polls` param). app.js: removed shippingLabelState/labelCaption/pollLabelStatuses/LABEL_BUSY maps + orphaned legacy label-modal job code (~200 lines, no callers); batch = sequential sync fetches then rows flip to Download; popup-safe tab reservation on single click; format picker kept. Desk queue flow + retry cron untouched (portal no longer uses them). Verified WSL+mock: label_check PASS, sync endpoint auto/A4/cached/cancelled/bad-format all pass, portal_check PASS. Shopee v2 docs re-verified (all 50 logistics endpoints): create→result(READY)→download is mandatory, no direct label-GET; `download_to_label` TW-only, job trio = warehouse batch labels, `get_shipping_document_data_info` = self-render fields only.

- [x] **Awaiting-Pickup report + ship-date filtering (Dianxiaomi controls)** (df1ae92, DEPLOYED 2026-07-21) — (1) Reports: new first card `pickup` "Awaiting Pickup" (`api/pickup_register.py` → `dispatch_register.execute_pickup`: order_status=processed AND shipped_at IS NULL, same 8 cols as dispatch; LIVE worklist — ignores global date window since date would filter on updated_at fallback and drop stale waiting orders); Dispatch Register moved to second card. (2) **Root-cause fix for "shipped today missing"**: OSI never requested a ship-time field — `response_optional_fields` lacked it and controller read nonexistent `ship_time` → `shipped_at` NULL on prod AND dev. Real Shopee field = **`pickup_done_time`** (now requested in `api/shopee/order.py`, documented in SHOPEE_API_NOTES "Order Detail Timestamps"; historical payloads lack it until re-fetched). New OSO field `ship_date_local` (Date, indexed, ingest via controller from pickup_done_time × region tz; patch `v22` backfill). (3) Dianxiaomi-style date-type selector (Order date / Ship date) in portal date panel — `date_type` param through get_orders/_date_scope/_stage_counts/poll; Shipped stage auto-defaults to Ship date (manual pick sticky until stage change); sort gains ship_newest/ship_oldest. Mock: `STATE.advance` stamps pickup_done_time on SHIPPED. New `devtools/ship_date_check.py` **6/6 PASS**; portal/label/perm checks + full_e2e all PASS. zh queued.

- [x] **Shopee-mirror stage chips + queue/history date model + search fix** (df1ae92, DEPLOYED 2026-07-21) — client-driven redesign: (1) Orders stage chips now mirror Seller Centre 1:1: `pending / to_process / processed / shipping / completed / cancelled / returns` (STAGES + `_stage` = pure status mapping, review predicates removed from staging; `fulfilment` = processed+shipping). (2) Date model: queue stages (pending/to_process/processed/returns) default **Any date** (live worklists — Shopee never date-hides queues), history stages default Today (shipping/completed keyed on ship date); `stageDateDefaults()` drives chip/tile/notif navigation. All Orders defaults Today. (3) **Search bypasses the date window** (was silently returning nothing for other-day orders — the "search doesn't filter" bug). (4) **Manual check/approve stripped from portal** per client (Pending = "go process in Shopee"); Mark-Checked UI, timeline check step, review stage all removed portal-side; desk review machinery + `require_order_review` gate kept dormant (full_e2e still exercises it). Dashboard tiles → pending/to_process/processed/shipping. `ship_date_check` **7/7** (new search-bypass case), portal_check ok, label_check PASS, full_e2e 6/6 (one flaky run: first `_sync_all` raced `gen_orders` on mock — pre-existing timing, rerun green). zh queued (incl. fixing `Processed`=已加工→已处理). Follow-up candidates: persist `ship_by_date` + deadline countdown col; osi-docs guide pages still describe Mark-Checked flow.

- [x] **Stage-true sort + terminal event dates** (df1ae92, DEPLOYED 2026-07-21) — (1) "Newest/oldest" sorts now mean the buyer-facing order date (`order_date_local desc, creation desc` — creation lags on webhook delays/backfills, demoted to tie-break); stage switch also sets default sort (shipping/completed → ship_newest). (2) New OSO fields `completed_date_local` / `cancelled_date_local` (Date, indexed): Shopee reports no completion/cancel time, so `order_service._upsert` stamps the OBSERVED status transition (payload update_time × store clock; first-sync-already-terminal rows stay NULL — flip date unknowable). Patch `v23` backfills history with a labelled proxy (region-shifted `updated_at`, fallback ship/order date). Completed/Cancellation stages filter on their own event date by default (`date_type` += completed/cancelled; date panel shows the extra option on those stages). `ship_date_check` **9/9** (new completed/cancelled transition-stamp cases w/ fresh-mock-order harness; probe/paging hardening), portal_check ok, full_e2e 6/6.

- [x] **Store display name = Shopee LOGIN name (client decision 2026-07-21)** — investigation: Shopee has two names (`get_shop_info.shop_name` = account login, may look auto-generated e.g. "6b8vebcr9u"; `get_profile.shop_name` = storefront display name). Client cross-checks stores by the LOGIN name → `store_name` keeps `get_shop_info.shop_name`, do NOT "fix" to the prettier profile name (a first-pass get_profile overlay was built then reverted on client input). `api/shopee/shop.get_profile` wrapper kept (documented, unused); mock `/api/v2/shop/get_profile` route returns "<name> Official" so an accidental overlay fails loudly; SHOPEE_API_NOTES "Shop Names" section records the decision. New `devtools/store_name_check.py` **3/3 PASS** (store_name == mock login name exactly; expired-token orphans skipped). Portal already maps docname→store_name everywhere (no code leak found); prod store_name values verified = login names, no prod action needed.

- [x] **Single-line shrink-to-fit SKU stamp + reports default** (df1ae92, DEPLOYED 2026-07-21) — stamp = ONE horizontal line "SKU: A x2; B x1; C x3" (`_sku_line`), NEVER wraps (client corrected an earlier wrap-at-edge reading — extra lines collided with label text). Fit = `_fit_stamp_font`: rule font_size is the preferred max; longer lists SHRINK the font to the box width (floor `MIN_STAMP_FONT=5pt`; only past floor capacity → `_clip` "..."; `textwrap.shorten` replaced — it dropped whole overlong SKU tokens). `rule.max_lines` unused by stamping now. Unit-tested (short=9pt untouched / long=floor+clip / extreme=clip); label_check + stamp_editor_check PASS. Reports page: `S.reportKey` default + disabled-key fallback → `pickup`.
- [x] **Stamp-rule freshness hardening** (df1ae92, DEPLOYED 2026-07-21) — client: saved stamp position sometimes applies to prints only after a "large delay". Investigated: save→read verified FRESH on dev at every layer (same process, fresh process via `devtools/stamp_stale_probe.py`, live gunicorn worker via authenticated HTTP) — no server-side staleness reproduced. Hardened anyway: `_get_sku_overlay_rule` now queries the `Shipping Document SKU Overlay Rule` child table directly (immune to any document/singles cache layer, incl. frappe-version differences on prod) instead of `frappe.get_single`. Most likely remaining cause = browser re-showing an old print PDF tab/download (print files are per-second unique, so a real re-click always regenerates). label_check + stamp_editor_check PASS. Note: dev-site Administrator password set to `osidevprobe123` for HTTP probing.
- [x] **WIP reports disabled on portal** (df1ae92, DEPLOYED 2026-07-21) — Reports page: only Awaiting Pickup + Dispatch Register clickable; Shipping Overview / Return Summary / Reconciliation / P&L cards greyed with "In progress 开发中" tag, no click (client request — half-finished numbers confused them). UI-only (`DISABLED_REPORTS` in app.js; run_report untouched, Finance page P&L unaffected); re-enable = delete key from the map.

### Offline mock-platform server — `mock_platform/` (dev-only, never shipped)
FastAPI fake of Shopee v2 so OSI builds + stress-tests offline. Boots, seeds shops/orders from `config.yaml`, smoke-tested green. Run: `uvicorn mockshopee.main:app --port 9000` (venv in `mock_platform/.venv`).
- [x] Core server: config-driven shops/items/orders, in-memory state, envelope, chaos middleware (latency / `error_busy` / 429+Retry-After)
- [x] Routers: auth (flat token), shop (get_shop_info), order (list cursor-paginated + detail)
- [x] `/__control` plane: `stats`, `shops`, `gen_orders`, `advance`, `config`, `reset`
- [ ] Routers: product (get_item_list/base_info/model_list)
- [x] **Router `logistics.py`** (5 endpoints: get_tracking_number GET + get_shipping_document_parameter / create_shipping_document / get_shipping_document_result / download_shipping_document POST) — lazy `MOCKTRK##########` allocation at READY_TO_SHIP+, pre-allocation returns empty tracking (exercises OSI waiting_allocation), doc result PROCESSING on 1st poll → READY, download = real one-page 4x6" PDF (valid xref, parses under bench pypdf incl. writer round-trip). Drip lifecycle now READY_TO_SHIP→PROCESSED→SHIPPED. Verified 19/19 TestClient checks.
- [x] Seed runner `online_store_integration/online_store_integration/_mock_seed.py` (DEV-ONLY, gitignored in OSI repo; run via `bench --site erp.localhost execute online_store_integration._mock_seed.run`). Helpers: `.report` (snapshot), `.diag` (one-order traceback), `.fixschema`. Runs on the mock at **port 9900** (9000 = Frappe socketio — do NOT use).
- [x] **End-to-end PASS:** mock → OSI = 3 stores × 25 = 75 orders, 44 products auto-created, 0 errors, multi-currency (MYR/VND/PHP). Found + fixed a latent OSI bug: `product_service.py` read `item_row.product_image` (an `Image` field = no DB column) → `AttributeError` on the new-product path; now safe `.get()`.
- WSL run scripts in `mock_platform/`: `start_mock.sh` (port 9900), `wsl_poll.sh`, `wsl_poll_bench.sh`, `killport.sh`. Mock venv: `/home/frappe/osi-mock-venv`. Mock served from `/mnt/c/...`.
- [x] **Mock route** `payment.py` (`get_escrow_detail`) + control `advance_bulk` (lifecycle driver).
- [x] **Live-traffic drip simulator** (root repo, **UNCOMMITTED as of 2026-07-17**) — `mockshopee/drip.py` `DripManager` (Poisson arrivals w/ diurnal shaping + time_scale, lifecycle aging READY_TO_SHIP→PROCESSED→SHIPPED→COMPLETED ± cancel/refund) + `push_client.py` (signed Shopee-style order-status pushes → OSI webhook, HMAC body-only). Control endpoints `/drip/start|stop|status`; config `push`/`drip` sections in `config.example.yaml`; needs `httpx`. OSI-side sim config: `devtools/sim_setup.py`.
- [x] **Committed E2E + stress harness** — `online_store_integration/devtools/` (NOT gitignored; guarded by `assert_mock_env` → refuses unless OSI `api_url` is local). Run via bench:
  - `devtools.setup.connect_and_seed` — point OSI at mock + seed Online Stores
  - `devtools.bootstrap.bootstrap` — idempotent ERPNext seed so SO/DN/SI creation works: back-dated Currency Exchange (mock-currency→company, rates overridable), allow-stale FX, allow-negative-stock, auto-create items + per-currency receivables, OSI ERP mapping, is_billing_contact drift check
  - `devtools.scenario.full_e2e` — **PASS/FAIL per stage:** orders→gate→approve→SO/DN/SI→escrow→return reversal→reports. **Verified 6/6 PASS.**
  - `devtools.scenario.stress` (kwargs: orders/batch/return_pct/latency_ms/error_rate) + `stress_quick`. Sequential throughput: approve→SO/DN/SI ~0.44s/order.
  - **Parallel-worker mode** (real multi-worker lock contention): `devtools.scenario.stress_parallel_enqueue` enqueues one ERP-creating job per order onto the `long` queue (`devtools/workers.py:approve_and_sync_one`); launch concurrency with `mock_platform/stress_workers.sh long N`; drain-poll with `mock_platform/stress_poll.sh`; report via `parallel_status` (queued/finished/failed + Error-Log deadlock/lock-wait scan). **Verified: 533 jobs × 5 workers → 0 deadlocks, 0 lock-waits, 0 failures.** Crank workers/orders/chaos to find the ceiling. (pkill of `bench worker --queue long` also stops the bench's own long worker → restart bench to restore.)
- [ ] Routers: product (get_item_list/base_info/model_list).

## Repo has two layers

1. **Root** = provisioning + runbook for a Frappe/ERPNext bench. Not application code.
   - `setup_bench.sh` — builds a bench: `bench init` → `bench get-app erpnext` → `bench get-app private_frappe_codespace` (the custom app) → `bench new-site` → `install-app`. Stable branch auto-detected from ERPNext git tags (`version-N`) unless `STABLE_BRANCH` set.
   - `README.md` — quick start for `setup_bench.sh`.
   - `BENCH_STARTUP.md` — **canonical runbook** for the local WSL bench (start, verify, asset/socket troubleshooting, VPS deploy). Read before starting/diagnosing the running bench.
2. **`online_store_integration/`** = the custom Frappe app ("OSI"). Its own git repo. Real development happens here. Has its own deep docs — read them, don't duplicate:
   - `PROJECT.md` — canonical OSI guide (data model, patches, fixtures, commands, guardrails).
   - `AGENTS.md` — shortest command reference.
   - `SHOPEE_API_NOTES.md` — **read before touching any Shopee API code.**

## Three layers / where things live (verified)

1. **Windows local clone** = `online_store_integration/` in this repo. The `.git` lives here (remote `upstream` → `github.com/Flynn-Mac-97/online_store_integration.git`, branch `main`). **All editing + all git happens here.**
2. **WSL dev bench** = distro `FrappeBench`, bench `/home/frappe/frappe-bench`, runs as user `frappe`, site `erp.localhost`. Its `apps/online_store_integration` is a **symlink to the Windows clone** → editing on Windows changes the WSL app instantly (no copy). There is no separate "WSL git". (Avoid the unrelated `online_store_integration.backup.*` dir in WSL.)
3. **VPS prod** = `47.84.1.78`, bench `/home/frappe/frappe-bench`, user `frappe`, site `frontend`. Same `upstream` remote. Deployed via `git pull`.

> Provisioning vs reality: `setup_bench.sh` (`frappe-bench` / `dev.localhost`) and BENCH_STARTUP.md (`/root/frappe_bench`) are both **stale**. The live WSL bench is `/home/frappe/frappe-bench` + `erp.localhost`, matching the OSI docs and helper scripts.

## Delegation & token budget (orchestrator policy)

Principle: keep bulk text (full files, big diffs, failed-edit echoes) OUT of the Opus orchestrator context. Opus plans/routes/decides/talks to user + runs cheap shell. Push reading, editing, searching, reviewing into cheap caveman subagents — their output is caveman-compressed (~60% smaller). Opus holds only intent + compressed receipts.

Routing (model = `Agent` tool `model` override):

| Task | Agent | Model |
|------|-------|-------|
| Locate code / "where is X" / map dir | `caveman:cavecrew-investigator` | haiku |
| Broad multi-area search (conclusion only) | `Explore` | haiku |
| 1–2 file surgical edit | `caveman:cavecrew-builder` | sonnet |
| Diff / branch / PR review | `caveman:cavecrew-reviewer` | haiku |
| 3+ file change | orchestrator splits → many `cavecrew-builder` | sonnet |
| Build / migrate / restart / curl poll | orchestrator `Bash` direct | — |
| Verify behavior end-to-end | `claude` / `general-purpose` | sonnet |

Loop: 1) locate (1–3 investigators parallel → `file:line` map; skip if paths known). 2) design — Opus decides scope + acceptance; no full-file reads, ranged `Read` only if truly needed. 3) edit — `cavecrew-builder`, one per 1–2 files; parallel + `isolation: worktree` when no cross-file deps, serial when dependent. 4) review — `cavecrew-reviewer`. 5) integrate — Opus runs build/migrate/restart, polls ready. 6) verify.

Builder brief always includes: exact target, acceptance criteria, compressed constraints (e.g. SHOPEE_API_NOTES rule), and **"match on small unique anchors; on match-fail shrink the anchor, never resend the whole block; for tail-of-file rewrites use `head -n N` + append."** (This is the rule that would have killed the 250-line failed-edit waste.)

Don't delegate: trivial one-liners (faster inline), decisions/design/user comms (Opus only), anything already small + known. **Rule of thumb: delegate when the task would otherwise dump >~100 lines of file/diff text into Opus context.** Do not spawn agents the user didn't ask for when inline is cheaper.

## End-to-end workflow (all commands verified working)

PowerShell↔WSL/SSH nested quoting is fragile (see hazards below). These forms work:

**1. Start WSL dev bench** — `bench start` must stay foreground or the WSL distro shuts down. Launch it as a background process that holds the session open:
```powershell
# run_in_background; keeps WSL alive while bench serves
wsl -d FrappeBench -u root -- su - frappe -c '/home/frappe/start-bench.sh'
```
Wait until ready (poll, don't guess):
```powershell
wsl -d FrappeBench -u root -- bash -lc 'curl -s -o /dev/null -w "%{http_code}\n" --max-time 5 http://erp.localhost:8000/api/method/ping'   # 200 = up
```

**2. Build on WSL after edits** — use a `frappe` *login* shell + full bench path (don't hand-export PATH through nested quotes, it breaks):
```powershell
wsl -d FrappeBench -u root -- su - frappe -c 'cd /home/frappe/frappe-bench && /home/frappe/.local/bin/bench build --app online_store_integration'
```
For Python/schema/fixture changes use `migrate` instead/also: `... && /home/frappe/.local/bin/bench --site erp.localhost migrate`.

**3. Push to git (from the Windows clone only)** — git lives in the Windows folder; the symlink means WSL needs nothing:
```powershell
# from C:\...\Frappe-ERPNext-Dev\online_store_integration
# ALWAYS first: fold queued Mandarin translations into zh.csv (see Translations below)
python scripts/osi-translations.py merge
git add -A; git commit -m "..."; git push upstream main
```

**Translations (multi-locale).** OSI uses the standard Frappe scheme: UI strings wrapped `_()` (Py) / `__()` (JS), translations in `online_store_integration/translations/<lang>.csv` (`"source","译文",""`). Shipped locales (MALACA spec): **zh** (primary, has content) + scaffolded empties `tl, th, vi, ms, hi, ja, es` (English is the source language, no CSV). Each locale folds from its own `<lang>.pending.csv` queue. Workflow:
- **While coding:** the moment you add a new translatable string, append it *with its Mandarin* to `online_store_integration/translations/zh.pending.csv` as `"Source","中文",""`. This is the always-on queue so you never rescan the whole codebase at commit. Auto-fill the Mandarin yourself (no separate review). Other locales fill in later (translator / future AI step) via their own `<lang>.pending.csv`.
- **At every commit (mandatory first step):** `python scripts/osi-translations.py merge` — folds **every** `<lang>.pending.csv` into its `<lang>.csv` (updates matching keys in place, appends new ones; never reorders existing rows), then clears the queues. Commit the changed `<lang>.csv` + reset `<lang>.pending.csv` together.
- `python scripts/osi-translations.py check` lists what's queued (per locale) without merging. `... scaffold` creates any missing locale CSVs.
- Deploy needs nothing extra — the `<lang>.csv` files are pulled by the normal VPS deploy; translations load after the existing `bench build` / `bench --site … clear-cache`.

**4. Deploy on VPS — pull + build:**
```powershell
# inspect first — prod may have uncommitted hotfixes
ssh -i $env:USERPROFILE\.ssh\codex_vps_ed25519 -o IdentitiesOnly=yes root@47.84.1.78 "su - frappe -c 'cd /home/frappe/frappe-bench/apps/online_store_integration && git status -sb && git log --oneline -2'"
# pull (resolve any dirty state first — see guardrail) + build with explicit node/nvm PATH
ssh -i $env:USERPROFILE\.ssh\codex_vps_ed25519 -o IdentitiesOnly=yes root@47.84.1.78 "su - frappe -c 'cd /home/frappe/frappe-bench/apps/online_store_integration && git pull upstream main'"
ssh -i $env:USERPROFILE\.ssh\codex_vps_ed25519 -o IdentitiesOnly=yes root@47.84.1.78 "su - frappe -c 'cd /home/frappe/frappe-bench && export PATH=/home/frappe/.nvm/versions/node/v24.15.0/bin:/usr/local/bin:/usr/bin:/bin:/home/frappe/.local/bin && bench build --app online_store_integration'"
```
VPS is **supervisorless** (runs plain `bench start`, no supervisor). Deploy is just: `git pull` → `bench build` → restart the `bench start` process. Run `bench --site frontend migrate` only when the pull changed DocType schema/patches/fixtures. `build` alone refreshes assets but not running Python — a Python-only change needs the `bench start` restart to take effect.

> **VPS guardrail:** the prod working tree often has uncommitted hotfixes made directly on the box. Always `git status` + `git diff` before pulling. If the local commit already supersedes them, discarding is safe (`git checkout -- <files>`), but confirm with the user before destroying prod-only changes — a clean fast-forward whose diff stat matches the discarded files confirms they were the same work.

## PowerShell → WSL / SSH quoting hazards (learned the hard way)
- Wrap the remote command so PowerShell doesn't expand it: outer `'single quotes'` for `bash -lc '...'`. PowerShell tries to parse `$var:` as a drive ref — a bare `$i:` in a double-quoted string is a parse error.
- `nohup ... &` inside a one-shot `wsl -- bash -lc` does **not** survive — the distro exits when the command returns. Hold the session open with a foreground `run_in_background` process instead.
- Don't `export PATH=...:$PATH` through layered `su -c '...'` quotes; the `$PATH`/`;` get mangled. Use `su - frappe` (login shell loads PATH) + absolute `bench` path, or a fully literal `export PATH=...` with no `$PATH`.
- Loops/`$()`/multiple `;` rarely survive the PowerShell→WSL hop. For "wait until ready", poll with a single `curl` in a Bash `run_in_background` until-loop rather than a bash `for`/`while` over wsl.
- The `localhost proxy ... not mirrored` WSL stderr line is harmless noise.

## Common commands (run from bench root, inside WSL/VPS as user `frappe`)

OSI ships helper scripts (preferred low-token path):

```bash
apps/online_store_integration/scripts/osi-migrate.sh   # bench --site erp.localhost migrate
apps/online_store_integration/scripts/osi-build.sh     # bench build --app online_store_integration
apps/online_store_integration/scripts/osi-smoke.sh     # list-apps + execute frappe.utils.now
apps/online_store_integration/scripts/osi-test.sh      # run-tests --app online_store_integration
```

Raw equivalents and others:

```bash
bench --site erp.localhost migrate                       # after schema/patch/fixture changes
bench build --app online_store_integration               # after JS/CSS/image changes
bench --site erp.localhost run-tests --app online_store_integration
bench --site erp.localhost run-tests --module online_store_integration.<...>.test_<x>   # single module
bench --site erp.localhost export-fixtures               # after UI changes meant to ship
bench --site erp.localhost console                       # Python console with site context
bench --site erp.localhost execute dotted.path.to.method
bench --site erp.localhost mariadb                       # SQL shell (read-only during investigation)
bench --site erp.localhost clear-cache
```

Lint/format: `ruff` (config in `online_store_integration/pyproject.toml`, tab indent, double quotes, line-length 110). Pre-commit config at `online_store_integration/.pre-commit-config.yaml`.

## OSI architecture (the part that needs multiple files to grasp)

Frappe is **metadata-driven**: business objects are DocTypes (folder of JSON metadata + optional `.py` controller + `.js`). Tables are `tab<DocType Name>`. Single DocTypes (`"issingle": 1`, e.g. **OSI Settings**) store values in `tabSingles`, not a row-per-doc table. Never alter DocType tables with raw SQL — change metadata, then `migrate`.

Core DocTypes (`online_store_integration/online_store_integration/doctype/`):
`online_store`, `online_product`, `online_sales_order` (+ `online_sales_order_item` child), `osi_settings` (single, admin-tunable config), `osi_sync_log`, `shipping_document_sku_overlay_rule`.

Code layers (`online_store_integration/online_store_integration/`):
- **`api/`** — whitelisted endpoints + integration code. `api/services/` holds vendor-neutral logic (order, product, stock, sync, token, erpnext_sales). `api/shopee/` isolates Shopee-specific code (auth, signing, http, order, logistics, product, push, returns, …). `api/utils/` helpers. **Keep vendor-specific detail under the vendor folder; shared behavior in services/utils.**
- **`tasks.py`** — scheduler entry points. They only **enqueue** RQ jobs (`frappe.enqueue`, deduplicated by job ID); actual work runs in workers (visible in System > Background Jobs). Config (enabled/intervals/scope) read from the OSI Settings singleton. Wired via `scheduler_events` in `hooks.py`: `sync_changed_stores` (every tick, self-throttles), `refresh_expiring_tokens` (hourly), `cleanup_old_sync_logs` (daily 3am).
- **`hooks.py`** — app integration surface: `app_include_js`, `scheduler_events`, `fixtures` (Role `OSI User`, Workspace Sidebar `eCommerce`, several Custom HTML Blocks). Prefer adding behavior here over editing framework code.
- **`patches/`** + `patches.txt` — ordered data migrations (`v1`…`v9`), split `[pre_model_sync]` / `[post_model_sync]`. New patches append to the correct section. Make idempotent; use Frappe APIs over raw SQL.
- **`fixtures/`** — narrow OSI-only records shipped with the app.
- **`public/`** — desk assets; rebuild after edits.

## Guardrails (from PROJECT.md / AGENTS.md)

- All normal edits stay in `apps/online_store_integration`. Do **not** edit `apps/frappe` or `apps/erpnext` unless the user explicitly asks — use OSI extension points (DocTypes, hooks, patches, fixtures, services, API modules, assets). If a task seems to need framework edits, pause and explain why OSI extension points are insufficient.
- Run `migrate` after schema/patch/fixture changes; `build` after asset changes; verify in browser at `http://erp.localhost:8000`.
- Treat DocType JSON edits as schema changes (verify with `migrate`). For renames/data moves, add a patch, not just JSON.
- Don't commit local HTML captures, DB exports, backups, or private site exports.
- The Shopee `get_shipment_manifest` endpoint in `api/shopee/logistics.py` + `api/sync.py` is **unverified / likely invalid** — see SHOPEE_API_NOTES.md before relying on it.
- **Module-doc JSON edits (Report/Workspace/etc.) only resync on `migrate` if the JSON `modified` timestamp is bumped** — stale-roles bug hit on report role gating; always bump `modified` when editing exported module docs.
- **`frappe.get_app_path` scrubs hyphenated path segments** (`osi-docs` → `osi_docs`); resolve files under `www/osi-docs/` relative to `__file__` (see `www/osi-docs/api.py`).
